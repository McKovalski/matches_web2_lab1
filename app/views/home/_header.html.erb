<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <%= link_to 'Matches', root_path, class:"navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to 'Standings', teams_path, class:"nav-link" %>
        </li>
        <% if @current_user.present? %>
          <li class="nav-item">
            <%= button_to "Logout", "/auth/logout", method: :get, data: { turbo: false }, class:"btn btn-secondary" %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= button_to "Login", "/auth/auth0", method: :post, data: { turbo: false }, class:"btn btn-secondary" %>
          </li>
        <% end %>
      </ul>
    </div>
    <% if @current_user.present? %>
      <div class="navbar-brand"> Hello <%= @current_user['nickname'] %> <% if @current_user['name'] == "velara8158@cadolls.com" %> (Admin) <% end %> </div>
    <% end %>
  </div>
</nav>